# Linter configuration (MIGRATED to backend/pyproject.toml - Ruff)
# This file is kept for backward compatibility.
# All linting, formatting, and type checking is now handled by Ruff.

# Bandit configuration (security scanning - not replaced by Ruff)
[bandit]
exclude_dirs = tests
skips = B101,B601
